apiVersion: v1
kind: Service
metadata:
  name: gabriellas-python-app
  labels:
    app: gabriellas-python-app
spec:
  type: ClusterIP
  ports:
    - port: 8080
      targetPort: 8080
      protocol: TCP
      name: http
  selector:
    app: gabriellas-python-app


